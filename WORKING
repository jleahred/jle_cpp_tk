net/Http_server
        remove on request (response parameter)
        instead it, it is better to add a method called  send_response
        or not, perhaps it's easier this way. Almost for start with
    files,
    test,
    example,
    doc

class Upper_case format???

move containers to folder cont


chrono
  uadjust time
  big adjust time per day

signal_slot trapping all exceptions???
test performance




YAML
  · alarm -> YAML
  · YAML -> alarm

JSON
